# <Model name> — hands-on guide

> Copy this file to `guides/<model-id>.md` (the model's `id` from `models.json`) and fill it in as you work. Delete nothing — failed attempts and their fixes are part of the guide.

## Goal

What we wanted out of this model, in one line (e.g. "remove the background from product photos in Node").

## Setup

Environment and install — the real commands as run:

```bash
$
```

## Running it

The minimal working example, Node-first when possible:

```js
```

## Results

What actually happened: output pasted, timings, quality notes. Real runs only.

## Gotchas

- What bit us, and the fix for each.

## Verdict

Does it earn its place in the index? Better alternatives? One honest paragraph.

## Index updates

Changes made to `models.json` / `docs/model-notes.md` because of this session (fields corrected, notes added).